<div id="frases">
    <h3 class="text-center">Frases Naran Xandul</h3>
    <div class="carrousel">
        {% for row in rows %}
            {% set row_classes = [
            default_row_class ? 'views-row',
            'panel',
            'panel-default',
            ] %}

            <div class="frases-item col-sm-4">
                {{ row.content }}
            </div>
        {% endfor %}
    </div>
</div>