<div class="cursos-page-block">
    <div class="pleca-seccion">
        <span class="category-cursos-item">La vida con tu beb&eacute; el 1er año</span>
    </div>
    <div class="carrousel">
        {% for row in rows %}
            {% set row_classes = [
            default_row_class ? 'views-row',
            'panel',
            'panel-default',
            ] %}

            <div class="curso-item col-sm-4">
                {{ row.content }}
            </div>
        {% endfor %}
    </div>
</div>