<div id="more-view" class="mt30">
  <div class="block PN3r0 text-bold ml10 text-upper">
    <span class="text-gray">Lo </span><span class="text-yellow">Más </span><span class="text-gray">Visto</span>
  </div>
  {% for row in rows %}
    {%
    set row_classes = [
    default_row_class ? 'views-row',
    'panel',
    'panel-default',
    ]
    %}

    <div class="nodeListItem col-md-4 col-xs-6">
      {{ row.content }}
    </div>
  {% endfor %}
</div>