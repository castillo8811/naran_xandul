<div id="cursos">
    <h3 class="text-center">Cursos</h3>
    <div class="carrousel">
        {% for row in rows %}
            {% set row_classes = [
            default_row_class ? 'views-row',
            'panel',
            'panel-default',
            ] %}

            <div class="curso-item col-sm-4">
                {{ row.content }}
            </div>
        {% endfor %}
    </div>
</div>